import { describe, it, expect } from 'vitest';

describe('math', () => {
    it('add numbers correctly', () => {
        expect(1 + 2).toBe(3);
    });

    it('subtract numbers correctly', () => {
        expect(3 - 2).toBe(1);
    });
});
