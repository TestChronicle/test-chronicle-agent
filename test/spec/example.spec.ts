import { describe, it, expect } from 'vitest';

describe('math', () => {
    it('adds numbers correctly', () => {
        expect(1 + 2).toBe(3);
    });

    it('subtracts numbers correctly', () => {
        expect(3 - 2).toBe(1);
    });
});
