import { describe, it, expect } from 'vitest';

describe('math', () => {
    it('adds numbers correctly', () => {
        expect(1 + 2).toBe(3);
    });

    it('multiplies numbers correctly', () => {
        expect(3 * 5).toBe(15);
    });
});
